# Skyforge API
A high-performance, production-ready API for managing celestial body data.

## Installation
1. Run `go mod init skyforge-api` to initialize the Go module.
2. Run `go get` to install all dependencies.

## Running the API
1. Run `go run src/main.go` to start the API server.
